[project]
name = "stitch-mcp"
version = "0.1.0"
description = "MCP Server for Google Stitch API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0"
]

[project.scripts]
stitch-mcp = "stitch_mcp.server:main"
